@Test
public void test_deleteConfiguration() {
    ConfigurationObj configObj = new ConfigurationObj();
    Mockito.when(configurationApiDelegateImpl.deleteConfiguration(Mockito.anyString(), Mockito.any(ConfigurationObj.class)))
           .thenReturn(ResponseEntity.ok().build());

    ResponseEntity<?> response = compositeDelegate.deleteConfiguration("testId", configObj);
    Assertions.assertNotNull(response);
}