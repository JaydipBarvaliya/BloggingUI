@Test
public void testEntityToDto_WhenEntityIsNull() {
    // Act
    ClientAppsObj result = clientAppsApiService.entityToDto(null);

    // Assert
    assertNotNull(result); // since MAPPER.dtoToConfig(null) still returns an object (depends on your mapper logic)
}